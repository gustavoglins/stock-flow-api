server:
  port: 8080

spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/db_stock_flow'
    username: 'postgres'
    password: 'postgres'
    driver-class-name: 'org.postgresql.Driver'
  jpa:
    hibernate:
      ddl-auto: update